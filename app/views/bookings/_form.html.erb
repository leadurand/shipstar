<%= simple_form_for([@ship, @booking]) do |f| %>
  <%= f.error_notification %>

  <%= f.input :start_at, label: "Fly from : " %>
  <%= f.input :end_at, label: "to : " %>

  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
